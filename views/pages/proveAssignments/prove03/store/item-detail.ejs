<!DOCTYPE html>
<html>

<head>
    <% include ../partials/header.ejs %>
</head>

<body>
    <% include ../partials/nav.ejs %>

    <div class="jumbotron text-center">
        <div class="container">
            <h1><%= title %></h1>
        </div>
    </div>
    <div>
        <div class="centered">
            <h2><%= item.title %></h2>
            <img src="<%  item.imgURL %>">
            <p>Price: <%= item.price%></p>
            <p>SKU: <% item.sku%></p>
            <p><%= item.decription %></p>
            <form method="POST" action="/assignments/prove03-store/cart">
                <button type="submit">Add to Cart</button>
                <input type="hidden" name="sku" vale="<%= item.sku %>">
            </form>
        </div>
    </div>
</body>

</html>